# Docker 網路基礎：Linux 網路構件與 bridge 模式

Docker 的 bridge 網路就是一台「裝在主機裡的虛擬交換器」：每個容器插一條虛擬網路線到這台交換器上，彼此互通；要出門則由主機當閘道、換上主機的 IP 出去。

第 01 章的 NET namespace 給了容器「自己的網路世界」，這一章補上世界之間的道路怎麼修。玄貓的教法是先徒手用 Linux 原生指令搭一座迷你交換器，再回頭看 Docker 的 bridge——你會發現 Docker 做的事你已經全部親手做過一遍。