# Docker Compose 實戰（二）：健康檢查、相依性與環境管理

健康檢查是「幫容器裝一支體溫計」：容器程序活著不代表服務真的能用，體溫計定期戳一下、確認它不只是喘氣、還真的能接客——然後 Compose 才敢把流量或依賴放心交給它。

第 13 章結尾埋了個大坑：`depends_on` 只保證「db 容器先啟動」，不保證「db 的資料庫真的能連」。web 搶在資料庫就緒前連線，直接吃閉門羹。這一章就把這個坑填平，順帶把重啟自癒、副本擴充、相依編排一次講透——全部落在可直接跑的 YAML 與驗證指令上。